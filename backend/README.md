Backend for AI Learning Portal

Setup

    cd backend
    npm install
    copy .env.example .env
    npm run dev

Notes
- Gemini integration is stubbed. Replace the stub in `src/services/geminiClient.ts`.
